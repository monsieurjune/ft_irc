:prefix FOO param :trailing \r\n

FOO => map(cmd, func(void *))

510 + 2

prefix cmd group_of_param trailing_param crlf

prefix = optional with : at first of irc msg
cmd = letter or 3 digit
group_of_param = optional but no :, \x20
trailing_param = optional but everything behind :

irc msg => no \0 at all